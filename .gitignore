# Node / Netlify function dependencies
node_modules/

# OS/editor noise
.DS_Store
Thumbs.db

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Environment files
.env
.env.*

# Netlify local tooling cache
.netlify/

# Ignore server data files to prevent overwriting live data on deploy
server/data/*.json
# Keep exception for structure if needed, but risky. For now, ignore all to be safe.
# !server/data/.keep

# Keep server/data tracked so VPS can receive account/settings updates via Git.
# (Commented out because this wipes live data)
# server/data/
